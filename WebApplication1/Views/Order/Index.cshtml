@model System.Collections.Generic.List<WebApplication1.Models.Order>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>Orders</h2>

<p>
    <a asp-action="Create">Create New Order</a>
</p>

@if (Model.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>Order ID</th>
                <th>Order Date</th>
                <th>Customer Name</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var order in Model)
            {
                <tr>
                    <td>@order.OrderID</td>
                    <td>@order.OrderDate.ToShortDateString()</td>
                    <td>@order.CustomerName</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@order.OrderID">Details</a> |
                        @* <a asp-action="Edit" asp-route-id="@order.OrderID">Edit</a> | *@
                        @* <a asp-action="Delete" asp-route-id="@order.OrderID">Delete</a> *@
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No orders found.</p>
}
